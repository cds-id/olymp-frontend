import type { Config } from "vike/types";

export default {
  title: "Mixed SSR+CSR Demo — Solid DDD Boilerplate",
  description:
    "Combined server-side rendering and client-side interactivity. Server data + counter, live clock, search filter.",
} satisfies Config;
