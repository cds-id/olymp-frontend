import type { Config } from "vike/types";

export default {
  ssr: false,
  title: "CSR Demo — Solid DDD Boilerplate",
  description: "Client-side only rendering demo. No server HTML — everything runs in browser.",
} satisfies Config;
